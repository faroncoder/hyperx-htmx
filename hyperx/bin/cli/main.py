"""
Auto-generated HyperX CLI main
──────────────────────────────
DO NOT EDIT — generated by scoffold_main.py
"""
import sys
import argparse
from hyperx.bin.cli.dispatcher import run_build
from hyperx.bin.cli.commands.install import run_install
from hyperx.bin.cli.commands.postinstall import run_postinstall


def dispatch(args):
    """Auto-generated dispatcher"""
    cmd = args.command
    if False: pass
    elif cmd == "build": run_build(args)
    elif cmd == "install": check_hyperx(args)
    elif cmd == "postinstall": run_postinstall(args)
    else:
        print("⚠️ Unknown or missing command. Use: hyperx --help")

def main():
    parser = argparse.ArgumentParser(prog="hyperx", description="Auto-generated HyperX CLI")
    sub = parser.add_subparsers(dest="command")

    sub.add_parser("build", help="Run build command")
    sub.add_parser("install", help="Run install command")
    sub.add_parser("postinstall", help="Run postinstall command")

    args = parser.parse_args()
    if not args.command:
        parser.print_help()
        sys.exit(0)
    dispatch(args)

if __name__ == "__main__":
    main()
